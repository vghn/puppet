---
# Mini role data

# Cron jobs
profile::base::cron_jobs:
  'Docker CleanUp':
    command: docker system prune --all --force 2>&1 | /usr/bin/logger -t DockerCleanUp
    minute: '2'
    hour: '2'

# Docker
docker::docker_users:
  - vlad

# Samba
samba::server::workgroup: 'WORKGROUP'
samba::server::server_string: 'Mini'
samba::server::netbios_name: 'Mini'
samba::server::domain_master: true
samba::server::security: 'user'
samba::server::map_to_guest: 'Bad User'
samba::server::extra_global_options:
  - 'wins support = yes'
  - 'min protocol = SMB2'
samba::server::shares:
  TV:
    - 'comment = TV'
    - 'path = /data/TV'
    - 'writeable = yes'
    - 'browseable = yes'
    - 'valid users = @sambashare'
    - 'create mask = 0664'
    - 'directory mask = 0775'
    - 'force group = sambashare'

# Miscellaneous
profile::misc::ignore_lid_switch: true

---
# Global Hiera data

# Essential packages
profile::base::Ubuntu::xenial::packages:
  - curl
  - htop
  - tmux
  - unzip
  - vim
  - wget

# Software versions
profile::jq::version: '1.5'
docker::compose::version: '1.9.0'

#!/usr/bin/env bash
# CI/CD Tasks

# Load environment
# shellcheck disable=1090
. "$(cd "$(dirname "${BASH_SOURCE[0]}")/../" && pwd -P)/envrc"

# Process arguments
case "${1:-}" in
  install)
    ci_install
    ;;
  test)
    ci_test
    ;;
  *)
    e_abort "USAGE: ${BASH_SOURCE[0]} [install | test "
    ;;
esac

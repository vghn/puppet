#!/usr/bin/env bash
# Docker Compose Run Script

# Load environment
# shellcheck disable=1090
. "$(cd "$(dirname "${BASH_SOURCE[0]}")/../" && pwd -P)/envrc"

#VARs
ENVNAME="${1:-prod}"

# Run docker compose
e_header "Running Docker Compose (${ENVNAME})"
docker-compose -f "${APPDIR}/docker-compose.${ENVNAME}.yml" up -d

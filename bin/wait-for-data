#!/usr/bin/env sh
# Wait for data to be deployed

until [ -f /var/local/data_deployed ]; do
  >&2 echo "Data did not deploy - sleeping"
  sleep 5
done

echo "Data deployed - executing command"
exec "$@"

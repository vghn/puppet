#!/usr/bin/env bash
# GIT Pre-Push Hook

# Load environment
# shellcheck disable=1090
. "$(cd "$(dirname "${BASH_SOURCE[0]}")/../../" && pwd -P)/envrc"

# Hooks
main(){
  # Pre-Push hook
  e_header 'GIT Pre-Push:'
  upload_private_data

  # Git Push
  e_header 'GIT Push:'
}

# Main
main "$@"
